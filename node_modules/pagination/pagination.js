
function paging(vTotalRows,res){
	vArr=123;
/*	
		var a = DataRows;
	    var vRows;
		var vTotal;
		var vPage;
		var vCountPages;
		var vCountPage;		
		var vGT;
		var vPageLimit;
			
        vRows   = 10;        
        vTotal  = vTotalRows;
        vPage = req.param('vPage');
        vCountPages = Math.round(vTotal/vRows);
        vGT = vCountPages/5; 
        MaxPage = vPage + 4;
        MinPage = vPage - 4;
        MaxNearPage = MaxPage - 1;
        
        if(vPage < 5){            
            if( MaxPage <= Math.round(vTotal/vRows) ) {    
                vCountPage = 5 ;
                vPageLimit = 1;                
            }
            else{                
                vCountPage = Math.round(vTotal/vRows) ;
                vPageLimit = 1;            
            }        
        }
        else{
            if( MaxPage < Math.round(vTotal/vRows) ) {                    
                vCountPage = Number(vPage) + 4 ;
                vPageLimit = Number(vPage)-1;                            
            }
            else{                
                vCountPage = Math.round(vTotal/vRows) ;
                vPageLimit = vPage-1;                        
            }
        }
        
		var vArr = {
				'vRows':vRows,
				'vTotal':vTotal,
				'vPage':vPage,
				'vCountPages':vCountPages,
				'vCountPage':vCountPage,		
				'vGT':vGT,
				'vPageLimit':vPageLimit,
				'vCurrentPage':vPage			
			}
*/			
		res.send(JSON.stringify(vArr));
}
